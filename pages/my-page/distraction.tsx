import LabelSettings from "@/components/tracking/label-settings";

/**
 * 앱 라벨 커스터마이즈 페이지 (설계 문서 §2.3).
 * 데스크탑 전용 화면 — 웹에서는 컴포넌트가 다운로드 안내를 대신 보여준다.
 */
export default function DistractionSettingsPage() {
  return <LabelSettings />;
}
